/**
 * @deprecated 请从 '@/stores/use-conversation-store' 导入。
 * 本文件保留为向后兼容的重新导出层，将在下次清理时删除。
 */
export type { ChatEntry, ConversationRecord, ConversationStore } from '@/stores/use-conversation-store';
export { useConversationStore } from '@/stores/use-conversation-store';
