# 2026-04-11 更新日志

## 0.4 Phase 4A：NodeStore 动态分组接线完成

在上一轮已经补出 NodeStore 分组适配层与测试基线之后，今天继续沿着 Phase 4A 做第二个小闭环：把默认节点商店视图真正接到动态分组 adapter，但仍然保留静态 fallback，不触碰 `workflow-meta.ts`，也不改变社区节点可见性。

### 完成内容
1. **NodeStore 分组接线切到 adapter**
   - `frontend/src/components/layout/sidebar/NodeStoreDefaultView.tsx`
   - 不再直接内联 `NODE_CATEGORIES`
   - 改为消费 `resolve-node-store-groups.ts` 的纯函数结果
2. **新增 View 层纯函数接口**
   - `ALL_NODE_STORE_CATEGORY_ID = 'all'`
   - `resolveNodeStoreGroupsForView(...)`
   - `resolveSelectedNodeStoreCategory(...)`
   - 继续保持当前 5 组产品语义：
     - 输入源
     - AI 处理
     - 内容生成
     - 输出 & 存储
     - 逻辑控制
3. **动态组与静态 fallback 规则已锁定**
   - `loading / error / empty manifest` → 回退到完整静态 5 组
   - manifest 可用且非空 → 只显示实际有节点的非空组
   - 当前选中的分组在动态结果里不存在时 → 自动回退到 `all`
4. **继续保留现有兼容边界**
   - 搜索过滤逻辑不变
   - `resolveNodeStoreCopy(...)` 继续负责 manifest-first 文案回退
   - `community_node` 与未来未知节点继续不进入默认商店
   - `unmappedManifestTypes` 只保留在逻辑层，不新增 UI 暴露

### 修改文件
- `frontend/src/components/layout/sidebar/NodeStoreDefaultView.tsx`
- `frontend/src/components/layout/sidebar/resolve-node-store-groups.ts`
- `frontend/src/__tests__/node-store-groups.property.test.ts`

### 新增测试覆盖
- `loading / error / empty manifest` 下回退到静态 5 组
- partial manifest 下只显示非空组
- 当前分组失效时回退到 `all`
- `community_node` / 未来未知节点继续进入 `unmappedManifestTypes`

### 验证结果
- `pnpm --dir frontend test -- src/__tests__/node-store-groups.property.test.ts src/__tests__/node-store-copy.property.test.ts src/__tests__/node-manifest.service.property.test.ts`
- 结果：`19 passed`

- `pnpm --dir frontend exec eslint src/components/layout/sidebar/NodeStoreDefaultView.tsx src/components/layout/sidebar/resolve-node-store-groups.ts src/__tests__/node-store-groups.property.test.ts`
- 结果：通过

### 提交
- `6b0f96d feat(frontend): wire node store dynamic groups`

## 0.3 Phase 4B：code-review-agent 规则集深化完成

今天在不触碰 Gateway、repo-aware、外部 LLM、其他 agent 目录的前提下，继续沿着 Phase 4B 做了一个单独可提交的小闭环：只深化 `code-review-agent` 的本地规则引擎与对应测试。

### 完成内容
1. **新增命令执行面规则**
   - 覆盖：
     - `subprocess(..., shell=True)`
     - `os.system(...)`
     - `child_process.exec(...)` / `execSync(...)`
2. **新增 TLS 校验关闭规则**
   - 覆盖：
     - `verify=False`
     - `rejectUnauthorized: false`
     - `ssl._create_unverified_context(...)`
3. **增强宽泛吞错识别**
   - 从只识别单行 `except Exception:`，
   - 扩展为可识别：
     - `except:`
     - `except Exception as e:`
     - 后续缩进块中的 `pass` / `continue` / `return None`
4. **保持输出与兼容边界不变**
   - 仍只输出 `Summary + Findings + Limitations`
   - 不引入 JSON findings
   - 不改 `/v1/chat/completions` 协议
   - 不改变 unified diff “只看新增行”的策略
   - 继续保留“同规则同文件只报首次命中”的去重策略

### 修改文件
- `agents/code-review-agent/src/core/agent.py`
- `agents/code-review-agent/tests/test_review_logic.py`

### 新增测试覆盖
- snippet 命中新规则
- multi-file diff 命中新规则并输出正确 `File: path:line`
- bare `except` + nested `continue` 命中宽泛吞错
- 同文件同规则只报一次
- `shell=False` / `verify=True` 等安全用法不误报

### 验证结果
- `pytest agents/code-review-agent/tests -q`
- 结果：`25 passed`

### 提交
- `79c9995 feat(code-review-agent): deepen heuristic review rules`

## 0. D2 / Task 3.2a：stores 目录重组兼容层完成

今天继续推进 Phase 3 前端重构，但只做了 `Task 3.2 stores 重组` 的最小闭环：先完成 stores 的物理归类与兼容导出层，不混入任何新行为，也不批量改业务 import。

### 完成内容
1. **stores 新目录结构已建立**
   - `frontend/src/stores/chat/`
   - `frontend/src/stores/workflow/`
   - `frontend/src/stores/ui/`
   - `frontend/src/stores/admin/`
2. **store 文件已物理迁移**
   - chat:
     - `use-ai-chat-store.ts`
     - `use-conversation-store.ts`
   - workflow:
     - `use-workflow-store.ts`
     - `execution-slice.ts`
     - `history-slice.ts`
   - ui:
     - `use-panel-store.ts`
     - `use-settings-store.ts`
   - admin:
     - `use-admin-store.ts`
3. **根层兼容 shim 已补齐**
   - 保留旧路径：
     - `@/stores/use-ai-chat-store`
     - `@/stores/use-conversation-store`
     - `@/stores/use-workflow-store`
     - `@/stores/use-panel-store`
     - `@/stores/use-settings-store`
     - `@/stores/use-admin-store`
   - 旧路径全部改为显式转发到新目录，不使用 `export *`
4. **新增统一 barrel**
   - 新增 `frontend/src/stores/index.ts`
   - 显式导出 chat / workflow / ui / admin 分组 store 与面板常量
5. **保守范围控制**
   - `workflow-store-helpers.ts` 保持原地不动
   - 没有批量替换业务调用方 import
   - 没有修改任何 store state shape / action / 持久化 key

### 新增测试
- `frontend/src/__tests__/store-path-compat.property.test.ts`

### 覆盖点
- 旧路径与新路径的 `useAIChatStore` / `useConversationStore` / `useWorkflowStore` 指向同一实例
- `usePanelStore` / `useSettingsStore` / `useAdminStore` 在旧路径与新路径下保持一致
- panel 常量在旧路径与新路径下保持一致
- `@/stores` barrel 可正常导出同一份 store 引用

### 验证结果
- `pnpm --dir frontend test -- src/__tests__/store-path-compat.property.test.ts src/__tests__/ai-chat-store.property.test.ts src/__tests__/chat-conversation-sync.property.test.ts src/__tests__/workflow-store.property.test.ts`
- 结果：`14 passed`

- `pnpm --dir frontend exec eslint src/stores/index.ts src/stores/use-admin-store.ts src/stores/use-ai-chat-store.ts src/stores/use-conversation-store.ts src/stores/use-panel-store.ts src/stores/use-settings-store.ts src/stores/use-workflow-store.ts src/stores/admin/use-admin-store.ts src/stores/chat/use-ai-chat-store.ts src/stores/chat/use-conversation-store.ts src/stores/ui/use-panel-store.ts src/stores/ui/use-settings-store.ts src/stores/workflow/use-workflow-store.ts src/stores/workflow/execution-slice.ts src/stores/workflow/history-slice.ts src/__tests__/store-path-compat.property.test.ts`
- 结果：通过

## 0.1 D2 / Task 3.2b-1：低风险 store import 切换完成

在 `3.2a` 完成目录重组与兼容层之后，今天继续推进 `Task 3.2`，但仍然只做最小闭环：把一批低风险调用方从旧路径 `@/stores/use-*` 切到新的分组直连路径，不改行为，不删除 shim。

### 完成内容
1. **tests 已切到新路径**
   - `ai-chat-store.property.test.ts`
   - `chat-conversation-sync.property.test.ts`
   - `workflow-store.property.test.ts`
   - `workflow-sync.property.test.ts`
   - `sse-store-update.property.test.ts`
2. **layout / sidebar 已切到新路径**
   - `usePanelStore` / `SidebarPanel` / `PINNABLE_PANELS` / `LEFT_PANEL_*` / `RIGHT_PANEL_*`
     - 统一改为 `@/stores/ui/use-panel-store`
   - `useSettingsStore`
     - 统一改为 `@/stores/ui/use-settings-store`
   - `useAIChatStore` / `useConversationStore`
     - 改为 `@/stores/chat/...`
   - `useWorkflowStore`
     - 改为 `@/stores/workflow/use-workflow-store`
3. **admin / settings 已切到新路径**
   - `features/admin/**` 中的 `useAdminStore`
     - 改为 `@/stores/admin/use-admin-store`
   - `features/settings/SettingsPageView.tsx`
     - `useSettingsStore` 改为 `@/stores/ui/use-settings-store`
4. **低风险 app 入口已切到新路径**
   - `app/(admin)/admin-analysis/login/page.tsx`
   - `app/(dashboard)/settings/page.tsx`
5. **兼容哨兵保留不动**
   - `store-path-compat.property.test.ts` 继续同时覆盖旧路径与新路径
   - 本轮没有删除任何 shim

### 当前收口效果
- 前端源码中的旧路径 `@/stores/use-*` 引用数量：
  - 从 `76` 降到 `46`
- 新分组路径 `@/stores/chat|workflow|ui|admin/use-*` 引用数量：
  - 从 `6` 增加到 `36`

### 验证结果
- `pnpm --dir frontend test -- src/__tests__/store-path-compat.property.test.ts src/__tests__/ai-chat-store.property.test.ts src/__tests__/chat-conversation-sync.property.test.ts src/__tests__/workflow-store.property.test.ts src/__tests__/workflow-sync.property.test.ts src/__tests__/sse-store-update.property.test.ts src/__tests__/admin-sidebar-navigation.property.test.ts src/__tests__/sidebar-navigation.property.test.ts src/__tests__/workflow-right-panel.property.test.ts`
- 结果：`31 passed / 127 passed`

### ESLint 说明
- 本轮对改动文件执行 ESLint 时，导入路径本身没有暴露新的错误类型。
- 但命中了两个目标文件里的既有规则问题，暂未在本轮处理：
  - `frontend/src/components/layout/sidebar/WorkflowExamplesPanel.tsx`
    - `react-hooks/set-state-in-effect`
    - `router` 未使用
  - `frontend/src/features/admin/shared/AdminSidebar.tsx`
    - `react-hooks/set-state-in-effect`
- 这些问题属于历史实现，不是本轮 import 切换引入；为了保持“小步推进”和“只改路径不改行为”，本轮没有顺手改逻辑。

## 0.2 D2 / Task 3.2b-2：workflow hooks 与 workspace shell import 切换完成

继续沿着 `Task 3.2` 推进，但依然坚持小步闭环：这一次只处理 `features/workflow/hooks` 与 `app/(dashboard)/workspace/[id]` 的 store import 路径，不进入 workflow components、MemoryView、EventBus 或 manifest-first。

### 完成内容
1. **workflow hooks 已切到新路径**
   - `chat-conversation-sync.ts`
     - conversation store 改为 `@/stores/chat/use-conversation-store`
   - 以下文件统一改为 `@/stores/workflow/use-workflow-store`
     - `use-action-executor.ts`
     - `use-canvas-clipboard.ts`
     - `use-canvas-context.ts`
     - `use-canvas-dnd.ts`
     - `use-canvas-event-listeners.ts`
     - `use-canvas-keyboard.ts`
     - `use-loop-group-drop.ts`
     - `use-workflow-execution.ts`
     - `use-workflow-sync.ts`
   - `use-stream-chat.ts`
     - `ChatEntry` / `useAIChatStore` 改到 `@/stores/chat/...`
     - `useWorkflowStore` 改到 `@/stores/workflow/use-workflow-store`
2. **workspace shell 已切到新路径**
   - `WorkflowCanvasLoader.tsx`
     - `useWorkflowStore` 改到 `@/stores/workflow/use-workflow-store`
   - `WorkflowPageShell.tsx`
     - `useWorkflowStore` 改到 `@/stores/workflow/use-workflow-store`
     - `useSettingsStore` 改到 `@/stores/ui/use-settings-store`
3. **小范围清理**
   - `use-action-executor.ts`
     - 删除两个未使用项：
       - `useCallback` import
       - `safePos(...)`
   - 这是为了让本轮 hooks/workspace shell 文件 ESLint 可通过，不涉及行为变更

### 明确保留不纳入
- `app/m/[id]/MemoryView.tsx`
- `features/workflow/components/**`
- `store-path-compat.property.test.ts`
- shim 删除
- EventBus / manifest-first / service consolidation

### 验证结果
- `pnpm --dir frontend test -- src/__tests__/chat-conversation-sync.property.test.ts src/__tests__/workflow-sync.property.test.ts src/__tests__/workflow-execution-closure.property.test.ts src/__tests__/loop-group-drop.property.test.ts src/__tests__/integration-fixes.workflow-runbutton.property.test.ts src/__tests__/workflow-right-panel.property.test.ts src/__tests__/store-path-compat.property.test.ts`
- 结果：`31 passed / 127 passed`

- `pnpm --dir frontend exec eslint src/features/workflow/hooks/chat-conversation-sync.ts src/features/workflow/hooks/use-action-executor.ts src/features/workflow/hooks/use-canvas-clipboard.ts src/features/workflow/hooks/use-canvas-context.ts src/features/workflow/hooks/use-canvas-dnd.ts src/features/workflow/hooks/use-canvas-event-listeners.ts src/features/workflow/hooks/use-canvas-keyboard.ts src/features/workflow/hooks/use-loop-group-drop.ts src/features/workflow/hooks/use-stream-chat.ts src/features/workflow/hooks/use-workflow-execution.ts src/features/workflow/hooks/use-workflow-sync.ts src/app/(dashboard)/workspace/[id]/WorkflowCanvasLoader.tsx src/app/(dashboard)/workspace/[id]/WorkflowPageShell.tsx`
- 结果：通过

## 1. Phase 2 后端重构后的测试基线已恢复

今日首先完成了 Workflow 路由测试的维稳修复，目标不是继续改生产逻辑，而是把 Phase 2 重构后的测试边界重新校准为可信基线。

### 完成内容
1. **Workflow Execute 测试 monkeypatch 边界重建**
   - 修复了 `backend/tests/test_workflow_execute_route_property.py` 因 `api/workflow_execute -> api/workflow/execute` 命名空间迁移导致的 patch 失效。
   - 补齐了对限流、日执行额度、usage ledger 等依赖的 patch，避免测试误穿透到真实 service。
2. **Workflow 路由测试 JWT 生成方式统一**
   - 新增 `backend/tests/_helpers.py::make_bearer_headers(...)`。
   - `test_workflow_crud_property.py`、`test_workflow_update_property.py`、`test_workflow_execute_route_property.py` 统一改为复用该 helper。
3. **回归验证通过**
   - Workflow 三类关键路由测试已恢复到可持续维护状态。

### 验证结果
- `pytest backend/tests/test_workflow_execute_route_property.py backend/tests/test_workflow_crud_property.py backend/tests/test_workflow_update_property.py -q`
- 结果：全部通过

## 2. B1 完成：非流式 AI Chat 已接入统一 usage 跟踪

在 Workflow Generate 试点通过后，今天继续把 `@track_usage` 推广到非流式 AI Chat 入口，目标是统一 assistant 类请求的 usage 生命周期。

### 完成内容
1. **增强 `track_usage(...)`**
   - 文件：`backend/app/services/usage_tracker.py`
   - 新增 `status_resolver` 能力。
   - 允许装饰器根据返回结果将请求状态归类为：
     - `completed`
     - `failed`
2. **重构 `/api/ai/chat`**
   - 文件：`backend/app/api/ai/chat.py`
   - 非流式 `/chat` 路由已改为“参数绑定 + decorator”模式。
   - 具体业务逻辑抽离到 `_ai_chat_impl(...)`。
   - 接入参数：
     - `source_type="assistant"`
     - `subtype_resolver=resolve_assistant_subtype`
     - `workflow_id_param="canvas_context.workflow_id"`
     - `status_resolver=_resolve_non_stream_chat_status`
3. **保持 `/api/ai/chat-stream` 原行为不变**
   - 流式端点继续沿用显式 `create_usage_request / finalize_usage_request` 生命周期。
   - 未在本轮改其 generator/finally 结构。

### 新增测试
- `backend/tests/test_ai_chat_usage_tracking_property.py`

### 覆盖点
- subtype 正确写入 `modify/chat`
- `canvas_context.workflow_id` 正确传给 usage request
- 正常返回时 finalize 为 `completed`
- 付费模型拒绝但返回 200 时 finalize 为 `failed`
- 业务异常抛出时 finalize 为 `failed`

### 验证结果
- `pytest backend/tests/test_ai_chat_usage_tracking_property.py -q`
- 结果：通过

## 3. D1 第一段完成：前端对话桥接已解耦

今天没有直接做大规模目录迁移，而是先把最危险的跨 store 副作用拆掉，建立前端桥接的稳定边界。

### 完成内容
1. **`useAIChatStore.pushMessage()` 收窄为纯 setter**
   - 文件：`frontend/src/stores/use-ai-chat-store.ts`
   - 现在只负责更新 AI chat store 内部 `history`。
   - 不再直接调用 conversation store。
2. **新增 conversation 持久化桥接 helper**
   - 文件：`frontend/src/features/workflow/hooks/chat-conversation-sync.ts`
   - 新增：
     - `ensureConversationReady()`
     - `persistConversationMessage(...)`
   - 负责在没有 active conversation 时先创建，再显式追加消息。
3. **对齐已知调用点**
   - `frontend/src/components/layout/sidebar/SidebarAIPanel.tsx`
     - user message 改为先 `pushMessage()`，再显式持久化
     - 快捷指令生成的 assistant message 也走同一路径
   - `frontend/src/features/workflow/hooks/use-stream-chat.ts`
     - 保留 `syncHistory()` 作为流式占位更新
     - assistant message 最终落盘改为显式 `persistConversationMessage(...)`

### 新增测试
- `frontend/src/__tests__/ai-chat-store.property.test.ts`
- `frontend/src/__tests__/chat-conversation-sync.property.test.ts`

### 验证结果
- `pnpm --dir frontend test -- src/__tests__/ai-chat-store.property.test.ts src/__tests__/chat-conversation-sync.property.test.ts`
- 结果：通过

## 4. D1 第二段完成：Workflow Service 浏览器侧认证路径已收口

为避免后续 Phase 3 持续扩散 header/cookie/token 三套并行调用方式，本轮先统一浏览器侧 workflow mutation 请求入口。

### 完成内容
1. **浏览器侧 mutation 统一走 shared fetch helper**
   - 文件：`frontend/src/services/workflow.service.ts`
   - 以下方法已收敛到 `credentialsFetch(...)`：
     - `updateWorkflow`
     - `deleteWorkflow`
     - `toggleLike`
     - `toggleFavorite`
     - `forkWorkflow`
2. **保留 server adapter**
   - 文件：`frontend/src/services/workflow.server.service.ts`
   - 继续承担 SSR / RSC 场景下的 token 注入与 refresh fallback。
   - 本轮未强行删除，避免 SSR 入口大换血。

### 新增测试
- `frontend/src/__tests__/workflow-service.property.test.ts`

### 覆盖点
- `updateWorkflow` 通过 shared helper 发送 JSON body
- `deleteWorkflow` 通过 shared helper 发起删除
- `like/favorite/fork` 通过 shared helper 发起 mutation
- 401 时继续抛出带 `status` 的 `ApiError`

### 验证结果
- `pnpm --dir frontend test -- src/__tests__/workflow-service.property.test.ts`
- 结果：通过

## 5. C1 完成：架构文档已同步到真实代码状态

文件：`.agent/ARCHITECTURE.md`

### 已同步内容
- Phase 2 后端命名空间：
  - `backend/app/api/workflow/`
  - `backend/app/api/ai/`
  - `backend/app/services/llm/`
- 非流式 usage 跟踪入口：
  - `backend/app/services/usage_tracker.py`
- 前端桥接现状：
  - `use-ai-chat-store.ts` 只保留 UI 状态
  - `chat-conversation-sync.ts` 承担 conversation 持久化桥接
  - `workflow.service.ts` 作为浏览器侧 workflow service 主入口
  - `workflow.server.service.ts` 作为 server adapter

## 6. 当前项目状态判断

截至 2026-04-11，项目状态可以明确分为三层：

1. **Phase 2 后端结构重构：完成**
   - 命名空间迁移已经完成，路由基线保持 `107`。
2. **Phase 2 后续维稳：基本完成**
   - Workflow 关键测试基线已恢复。
   - AI Chat 非流式 usage 收口已完成。
3. **Phase 3 前端重构：已切入第一段，但尚未全面展开**
   - 已完成最关键的 store 副作用拆解和 workflow service 请求收口。
   - 尚未开始 stores 子目录搬迁、EventBus、manifest-first 前端适配。

## 7. 下一步建议

建议按以下顺序继续推进：

1. **先做 D2 / Task 3.2：stores 目录重组**
   - 在当前副作用已经拆开的前提下，重组目录的风险显著下降。
2. **再做 D2 / Task 3.4：TypedEventBus**
   - 让跨组件同步从直接 store 串线，升级为更清晰的事件边界。
3. **最后做 D2 / Task 3.5：manifest-first 前端适配**
   - 该阶段会涉及 backend manifest schema 补充，适合作为独立闭环推进。

## 8. 今日验证汇总

### 后端
- `pytest backend/tests/test_ai_chat_usage_tracking_property.py backend/tests/test_workflow_execute_route_property.py backend/tests/test_workflow_crud_property.py backend/tests/test_workflow_update_property.py -q`
- 结果：`11 passed`

### 前端
- `pnpm --dir frontend test -- src/__tests__/ai-chat-store.property.test.ts src/__tests__/chat-conversation-sync.property.test.ts src/__tests__/workflow-service.property.test.ts`
- 结果：`6 passed`

### 静态检查
- `pnpm --dir frontend exec eslint ...`
- 结果：本轮变更文件通过

## 9. Phase 4B 首个真实闭环：Agent 最小可运行样板已落地

在 Phase 2 / Phase 3 官方收尾口径已经明确之后，这一轮没有回头继续拆 P2/P3 敏感尾项，也没有去顺手处理 Phase 4A 的 NodeStore 动态分组，而是正式切入当前默认主线：**Phase 4B Agent 样板**。

### 9.1 改动前的真实状态

开始前的 `agents/` 目录真实情况非常早期：

1. `agents/_template/` 不存在
2. `agents/code-review-agent/` 只有 README
3. `agents/deep-research-agent/` 与 `agents/news-agent/` 只有 README / MIGRATION 文档
4. 仓库里没有：
   - `backend/config/agents.yaml`
   - Agent Gateway
   - `/api/agents/*`

因此，这一轮的正确目标不是“一口气做完整 Agent 平台”，而是先补出：

- 可复制的最小模板
- 可运行的首个实例 Agent
- 最小契约测试

### 9.2 `agents/_template/` 已新增最小可运行骨架

本轮新建了：

- `agents/_template/src/__init__.py`
- `agents/_template/src/main.py`
- `agents/_template/src/config.py`
- `agents/_template/src/router.py`
- `agents/_template/src/endpoints/health.py`
- `agents/_template/src/endpoints/models.py`
- `agents/_template/src/endpoints/completions.py`
- `agents/_template/src/middleware/auth.py`
- `agents/_template/src/core/agent.py`
- `agents/_template/src/schemas/request.py`
- `agents/_template/src/schemas/response.py`
- `agents/_template/tests/conftest.py`
- `agents/_template/tests/test_contract.py`
- `agents/_template/.env.example`
- `agents/_template/requirements.txt`
- `agents/_template/README.md`

它现在已经具备：

1. 固定入口 `python -m src.main`
2. `pydantic-settings` 配置读取
3. `GET /health`
4. `GET /v1/models`
5. `POST /v1/chat/completions`
6. non-stream JSON 响应
7. SSE stream 响应
8. API Key 校验
9. `X-Request-Id` 响应头透传
10. 最小契约测试

### 9.3 `agents/code-review-agent/` 已从 README 变成真实实例

本轮给 `code-review-agent` 新增了同构骨架：

- `agents/code-review-agent/src/__init__.py`
- `agents/code-review-agent/src/main.py`
- `agents/code-review-agent/src/config.py`
- `agents/code-review-agent/src/router.py`
- `agents/code-review-agent/src/endpoints/health.py`
- `agents/code-review-agent/src/endpoints/models.py`
- `agents/code-review-agent/src/endpoints/completions.py`
- `agents/code-review-agent/src/middleware/auth.py`
- `agents/code-review-agent/src/core/agent.py`
- `agents/code-review-agent/src/schemas/request.py`
- `agents/code-review-agent/src/schemas/response.py`
- `agents/code-review-agent/tests/conftest.py`
- `agents/code-review-agent/tests/test_contract.py`
- `agents/code-review-agent/.env.example`
- `agents/code-review-agent/requirements.txt`

同时更新了：

- `agents/code-review-agent/README.md`

具体定制点只有三类：

1. 默认配置固定为：
   - `agent_name=code-review`
   - `model_id=code-review-v1`
   - `port=8001`
2. `src/core/agent.py`
   - 当前返回 deterministic code review stub
   - 不依赖外部 LLM
   - 不访问网络
3. README
   - 从“待实现”改成“最小可运行样板已落地”

### 9.4 当前最小协议行为

这轮不是只补目录，而是把最小协议行为一起锁住了：

1. `GET /health`
   - 返回 `status`
   - 返回 `agent`
   - 返回 `version`
   - 返回 `uptime_seconds`
   - 返回 `models`
2. `GET /v1/models`
   - 返回 OpenAI-compatible `object=list`
3. `POST /v1/chat/completions`
   - 支持 non-stream
   - 支持 stream
   - SSE 结尾固定为 `data: [DONE]\n\n`
4. 已实现错误分支：
   - `invalid_api_key`
   - `missing_model`
   - `empty_messages`
   - `model_not_found`

### 9.5 验证结果

已明确通过：

- `pytest agents/_template/tests -q`
  - 结果：`7 passed`
- `pytest agents/code-review-agent/tests -q`
  - 结果：`7 passed`

入口导入 smoke 也已确认通过：

1. `_template`
   - `from src.main import app` 可导入
2. `code-review-agent`
   - `from src.main import app` 可导入

已知仅有一个环境级 warning：

- pytest 尝试写 workspace 根目录 `.pytest_cache` 时出现权限 warning
- 不影响本轮测试通过结论

### 9.6 明确保留未做项

本轮刻意没有纳入：

1. `backend/config/agents.yaml`
2. Agent Gateway
3. `/api/agents/*`
4. Docker / compose
5. `pyproject.toml`
6. 真实上游 Provider 接入
7. 其他 agent 的迁移或实现
8. Phase 3 冻结边界
9. Phase 4A 的 NodeStore 动态分组

这不是遗漏，而是为了保证当前闭环可以：

- 单独验证
- 单独提交
- 单独回退

### 9.7 提交

- `29c4151 feat(agents): scaffold minimal runnable agent template`

### 9.8 当前判断

截至本轮，Phase 4B 已经从“目录规划和 README 说明”推进到了“真实存在的最小可运行基线”。

后续继续推进 4B 时，正确方式应是：

1. 基于 `_template` 继续扩展样板能力
2. 基于 `code-review-agent` 继续把 stub 替换为真实审查逻辑
3. 仍然不要在同一波次里把 Gateway、其他 agent、Docker、Provider 接线混在一起

## 10. Phase 4A 官方节点 version/changelog 治理基线已落地

在默认主线转回 Phase 4A 之后，这一轮没有继续扩 NodeStore 语义，也没有抢跑 Gateway/Wiki，而是把“节点 version 字段已存在，但没有真实治理”的缺口补成了一个独立闭环。

### 10.1 本轮前的真实状态

开始前，节点体系里虽然已经有：

1. `backend/app/nodes/_base.py`
   - `version = "1.0.0"`
2. `/api/nodes/manifest`
   - 已向前端返回 `version`

但仍然缺少关键治理基础：

1. 19 个官方节点类都没有显式声明自己的 `version`
2. 没有 `changelog` 字段
3. manifest 契约无法区分“类级治理声明”与“仅继承 BaseNode 默认值”
4. 前端类型里也没有 `changelog`

所以这轮的目标不是“做版本升级系统”，而是先把**显式声明 + 契约可见 + 测试锁定**做出来。

### 10.2 本轮已完成的代码闭环

本轮已完成：

1. `backend/app/nodes/_base.py`
   - 新增 `changelog: ClassVar[dict[str, str] | None] = None`
   - `get_manifest()` 现稳定返回 `changelog`
2. 19 个官方节点类
   - 全部显式声明：
     - `version = "1.0.0"`
     - `changelog = {"1.0.0": "初始版本"}`
3. `community_node`
   - 继续不纳入官方治理基线
   - manifest 中 `changelog` 保持 `null`
4. 前端类型
   - `NodeManifestItem` 新增 `changelog: Record<string, string> | null`
5. 前端 fixture 测试
   - 已同步补齐 `changelog`

这意味着：官方节点的版本信息不再只是“从 BaseNode 默认继承的一段字符串”，而是第一次在各节点类上形成了可检查、可演进的显式治理入口。

### 10.3 本轮新增/更新的测试

后端：

1. `backend/tests/test_node_manifest_contract_property.py`
   - 不再把“所有节点 version 固定等于 1.0.0”当作唯一契约含义
   - 改为断言语义化版本格式
   - 官方节点必须返回非空 `changelog` 且包含当前版本 key
   - `community_node` 的 `changelog` 必须为 `null`
2. `backend/tests/test_node_version_governance_property.py`
   - 新增治理锁定测试
   - 断言 19 个官方节点类都在 `__dict__` 中显式声明 `version` 与 `changelog`

前端：

1. `node-manifest.service.property.test.ts`
2. `node-store-groups.property.test.ts`
3. `canvas-node-copy.property.test.ts`
4. `execution-node-copy.property.test.ts`

以上 fixture 均已同步 `changelog`，确保 manifest 契约变更不会在前端被静默绕过。

### 10.4 实际验证结果

已通过：

- `pytest backend/tests/test_node_manifest_contract_property.py backend/tests/test_node_version_governance_property.py -q`
  - 结果：`3 passed`

- `pnpm --dir frontend test -- src/__tests__/node-manifest.service.property.test.ts src/__tests__/node-store-groups.property.test.ts src/__tests__/canvas-node-copy.property.test.ts src/__tests__/execution-node-copy.property.test.ts`
  - 结果：`4 files passed / 26 tests passed`

### 10.5 当前边界

这轮仍然**没有**进入以下范围：

1. 节点版本升级策略
2. 版本比较工具
3. changelog 前端展示
4. community node 版本治理合流
5. `workflow-meta.ts` 职责收缩

因此，本轮完成后的正确口径是：

1. Phase 4A 的官方节点版本治理基线已落地
2. 但这只是“显式声明 + manifest 契约 + 测试锁定”
3. 还不等于已经具备完整的版本迁移与展示体系

### 10.6 提交

- `7694e8f feat(nodes): add version governance baseline`

## 11. Phase 4B `code-review-agent` 已支持 repo-aware 前置输入能力

在 `Phase 4` 官方文档状态同步完成之后，这一轮重新回到当前主线：**Phase 4B 深化**。但范围仍然严格控制在 `code-review-agent` 内部，不碰 Gateway、不改冻结契约、不读取本地工作区。

### 11.1 本轮目标

本轮不是直接做“完整 repo-aware 审查”，而是先把 repo 上下文的**输入承载方式**固定下来：

1. 继续保持 `POST /v1/chat/completions` 的 OpenAI-compatible body 不变
2. 继续只消费最后一条 `user` 消息
3. 允许调用方在这条 `user` 消息中封装：
   - `<review_target path="...">...</review_target>`
   - `<repo_context path="...">...</repo_context>`
4. 规则审查仍然只扫描 `review_target`
5. `repo_context` 只作为辅助上下文，不单独产出 findings

这样做的价值是：先把“仓库上下文如何进 Agent”稳定下来，但不把当前样板推进到协议扩展、文件系统读取或 Gateway 集成。

### 11.2 本轮已完成的代码闭环

核心文件：

- `agents/code-review-agent/src/core/agent.py`

本轮已完成：

1. 新增结构化块解析
   - 支持从最后一条 `user` 消息中提取首个 `review_target`
   - 支持提取零到多个 `repo_context`
2. 保持 legacy 回退
   - 如果没有 `review_target`，继续按旧逻辑，把整条 `user` 消息作为 review target
   - 如果标签 malformed / 未闭合 / 空内容，不抛错，直接 fail-open 回退到 legacy 行为
3. 增强 review 输出
   - 当 `review_target path` 存在时，普通 snippet / plain_text findings 也会输出 `File: path:line`
   - 对没有 diff header 的 unified diff，优先使用 `review_target path` 替代 `<unknown>`
   - 当存在 `repo_context` 时，`Summary` 增加 `Context files supplied: N`
4. 更新 Limitations 口径
   - 明确写出已支持结构化 repo context 输入
   - 但当前仍无跨文件控制流/全仓库推理，也不会对 context 文件单独报问题

### 11.3 本轮新增测试

文件：

- `agents/code-review-agent/tests/test_review_logic.py`

本轮补齐了 repo-aware 输入基线相关用例：

1. legacy 非结构化输入不回归
2. `<review_target path="...">` 下普通 snippet findings 能输出 `File: path:line`
3. headerless diff + `review_target path` 时，不再回退到 `<unknown>`
4. 多个 `repo_context` 时，Summary 能显示 `Context files supplied: N`
5. `repo_context` 自身命中危险模式时，不进入 findings
6. malformed `review_target` 安全回退到 legacy 解析
7. `complete()` 仍只读取最新一条 `user` 消息，不跨消息聚合 repo context

### 11.4 实际验证结果

已通过：

- `pytest agents/code-review-agent/tests -q`
  - 结果：`30 passed`

### 11.5 当前边界

这轮仍然没有进入以下范围：

1. 不改 `request.py` schema
2. 不改 `agent-architecture.md` / `agent-gateway-contract.md`
3. 不读取本地仓库文件
4. 不让 `repo_context` 单独产出 findings
5. 不接外部 LLM
6. 不碰 Gateway / `agents.yaml` / `/api/agents/*`

因此，本轮后的正确口径是：

1. `code-review-agent` 已具备 **repo-aware 前置输入能力**
2. 但仍然只是“调用方显式喂上下文”的第一步
3. 还不是完整 repo-aware 审查，更不是平台集成

### 11.6 提交

- `55a1cc8 feat(code-review-agent): support repo-aware input envelope`

## 12. Phase 4B `code-review-agent` findings 纯文本输出模板已稳定化

在 repo-aware 前置输入基线落地之后，这一轮没有继续扩协议，也没有提前做 JSON findings，而是先把 `code-review-agent` 的 assistant 文本输出模板锁定下来，避免后续继续深化 4B 时出现“内容能看懂，但字段顺序和格式不断漂移”的问题。

### 12.1 本轮目标

本轮要解决的不是规则数量，而是**输出可依赖性**：

1. 保持 `Summary / Findings / Limitations` 三段式不变
2. 不改 `POST /v1/chat/completions` 的 OpenAI-compatible 契约
3. 不引入结构化 JSON findings
4. 把 `Summary` 字段顺序、`Findings` 模板、无命中形态和 findings 排序全部锁定到测试里

### 12.2 本轮已完成的代码闭环

核心文件：
- `agents/code-review-agent/src/core/agent.py`

本轮已完成：

1. `Summary` 字段顺序固定
   - 固定输出：
     - `- Input type: ...`
     - `- Files reviewed: N`
     - `- Reviewed lines: N`
     - `- Context files supplied: N`
     - `- Findings found: N`
   - 当结构化输入里存在 `review_target path` 时，额外输出：
     - `- Review target path: ...`
2. `Findings` 输出模板固定
   - 每条命中统一输出：
     - `Title`
     - `Rule ID`
     - `Severity`
     - `File`
     - `Evidence`
     - `Fix`
   - 无命中时统一输出：
     - `- None`
     - `Note: No deterministic findings...`
3. findings 排序固定化
   - 先按 `severity`（`high -> medium -> low`）
   - 再按 `file_path`
   - 再按 `line_number`
   - 再按 `position`
   - 最后按 `rule_id`
4. `File:` 行模板固定
   - 没有文件路径时不再省略，而是固定输出 `<none>`
5. 现有边界保持不变
   - heuristics 仍只分析 `review_target`
   - `repo_context` 仍只影响 summary/context 计数，不单独产生 findings

### 12.3 本轮补齐的测试

核心文件：
- `agents/code-review-agent/tests/test_review_logic.py`
- `agents/code-review-agent/tests/test_contract.py`

本轮补齐并锁定了以下场景：

1. legacy snippet 下 `Summary` 固定字段顺序，且始终输出 `Context files supplied: 0`
2. 结构化 `review_target path` 下，snippet findings 输出稳定的 `File: path:line`
3. headerless diff + `review_target path` 时，不再落到 `<unknown>`
4. no-findings 场景固定输出 `Findings -> - None -> Note: ...`
5. multi-file diff 的 findings 排序稳定，不随输入顺序抖动
6. contract test 额外锁定 `Rule ID / Severity / File` 等稳定标签

### 12.4 实际验证结果

已通过：
- `pytest agents/code-review-agent/tests -q`
  - 结果：`31 passed`

### 12.5 当前边界

这轮仍然**没有**进入以下范围：

1. 不改 HTTP response shape
2. 不新增 JSON findings
3. 不改 `request.py` / `response.py`
4. 不做 repo context 的单独问题输出
5. 不做跨文件控制流分析
6. 不接外部 LLM

因此，本轮后的正确口径是：

1. `code-review-agent` 已具备更稳定、可依赖的纯文本输出模板
2. 但仍然是纯文本三段式，不是结构化 findings API
3. repo-aware 现在只到“输入可承载 + 输出可稳定消费”的阶段，还未进入真正的跨文件推理

### 12.6 提交

- `af52b7e feat(code-review-agent): stabilize findings text format`

## 13. Phase 4B `code-review-agent` 外部 LLM 预留层已建立

在 findings 纯文本模板稳定化之后，这一轮没有直接接外部模型，也没有抢跑 Gateway，而是先把 `code-review-agent` 内部的 review backend seam 抽出来，为下一轮真实 upstream LLM 接入建立稳定入口。

### 13.1 本轮目标

本轮目标不是“已经接入外部 LLM”，而是先把后续接入点设计成可落地、可测试、可回退的内部结构：

1. 不改 `POST /v1/chat/completions` 协议
2. 不改 `request.py` / `response.py`
3. 不发真实网络请求
4. 默认行为继续保持本地 heuristics
5. 允许通过 agent 自己的配置切换到一个保留 backend，但当前仍立即回退到本地审查

### 13.2 本轮已完成的代码闭环

核心文件：
- `agents/code-review-agent/src/config.py`
- `agents/code-review-agent/src/core/agent.py`
- `agents/code-review-agent/src/core/upstream_review.py`
- `agents/code-review-agent/src/endpoints/completions.py`

本轮已完成：

1. 新增 review backend 配置入口
   - `review_backend: heuristic | upstream_reserved`
   - `upstream_model`
   - `upstream_base_url`
   - `upstream_api_key`
   - `upstream_timeout_seconds`
2. `CodeReviewAgent` 内部新增 backend seam
   - `heuristic` 继续复用当前本地规则审查
   - `upstream_reserved` 只构建 upstream request，然后立即回退到 `heuristic`
3. 抽出共享准备步骤
   - 最新 `user` 消息提取
   - structured payload 解析
   - `review_input` 构建
   - 后续 backend 共用的准备结果
4. 新增 upstream prompt/request builder
   - 统一构建 future OpenAI-compatible SDK 可用的 `messages`
   - 固定 system prompt
   - 把 `review_target` 与 `repo_context` 规范化写入 request payload
5. endpoint 已接配置
   - `/v1/chat/completions` 现在会把 `Settings` 中的 backend 配置传给 agent
   - 但默认仍走 `heuristic`

### 13.3 本轮补齐的测试

核心文件：
- `agents/code-review-agent/tests/test_review_logic.py`
- `agents/code-review-agent/tests/test_contract.py`

本轮补齐并锁定了以下场景：

1. 显式 `heuristic` backend 与默认输出完全一致
2. `upstream_reserved` backend 下输出与 `heuristic` 保持一致
3. legacy 非结构化输入仍按原逻辑进入准备步骤
4. malformed 结构化输入仍安全回退到 legacy 准备路径
5. upstream request builder 稳定包含：
   - `review_target path`
   - `repo_context` 文件路径
   - 原始 target 内容
6. contract 层确认启用 `upstream_reserved` 时：
   - non-stream 接口仍正常
   - SSE 接口仍正常

### 13.4 实际验证结果

已通过：
- `pytest agents/code-review-agent/tests -q`
  - 结果：`38 passed`

### 13.5 当前边界

这轮仍然**没有**进入以下范围：

1. 不接真实外部 LLM
2. 不新增网络依赖
3. 不改 `requirements.txt`
4. 不改 Gateway
5. 不改 `agents.yaml`
6. 不改 `/api/agents/*`

因此，本轮后的正确口径是：

1. `code-review-agent` 已具备外部 LLM 接入的内部预留层
2. 默认行为仍是本地 heuristics
3. 下一轮如果继续推进，可以在这个 seam 上接真实 upstream non-stream 调用，而不必再重做内部结构

### 13.6 提交

- `539f616 feat(code-review-agent): add upstream review seam`

## 14. Phase 4B `code-review-agent` 已接通真实 upstream non-stream 调用

在外部 LLM 预留层建立之后，这一轮没有扩大到 Gateway，也没有一步做到 provider streaming，而是沿着已经存在的 seam，只把 `code-review-agent` 的 **真实 OpenAI-compatible non-stream 上游调用** 接通，并把失败路径全部收口到严格回退。

### 14.1 本轮目标

本轮目标不是“平台级 Agent 集成”，而是把 `code-review-agent` 从“有预留层”推进到“真实上游调用第一段可用”：

1. 继续不改 `POST /v1/chat/completions` 协议
2. 继续不碰 Gateway / `agents.yaml` / `/api/agents/*`
3. 只做 non-stream 上游调用
4. 默认行为继续保持 `heuristic`
5. live backend 成功时也必须回到当前稳定文本模板，而不是把上游文本原样透出

### 14.2 本轮已完成的代码闭环

核心文件：
- `agents/code-review-agent/src/core/upstream_review.py`
- `agents/code-review-agent/src/core/agent.py`
- `agents/code-review-agent/src/config.py`
- `agents/code-review-agent/.env.example`
- `agents/code-review-agent/requirements.txt`

本轮已完成：

1. 新增真实 live backend
   - `review_backend` 现在支持：
     - `heuristic`
     - `upstream_reserved`
     - `upstream_openai_compatible`
2. 接入真实 OpenAI-compatible non-stream 调用
   - 使用 `AsyncOpenAI(base_url, api_key, timeout)`
   - 只在 `upstream_model / upstream_base_url / upstream_api_key` 完整时才尝试调用
3. 上游输出改为内部专用 JSON findings
   - `upstream_review.py` 现在要求上游返回：
     - `findings: [{title, rule_id, severity, file_path, line_number, evidence, fix}]`
   - agent 只消费内部 findings，不信任上游生成 `Summary / Limitations`
4. 成功也归一化
   - 上游成功后，findings 会转换成当前本地 `ReviewFinding`
   - 继续复用现有排序与 `format_review(...)`
   - 对外返回仍是稳定的 `Summary + Findings + Limitations` 纯文本模板
5. 严格回退已落地
   - 以下情况全部直接回退到 `heuristic`：
     - 配置缺失
     - SDK / HTTP 异常
     - 超时
     - 空内容
     - 非法 JSON
     - finding 字段不合法
6. `stream=True` 保持现有策略
   - 本轮不做 provider streaming
   - 先完成 upstream non-stream 或 fallback
   - 再把最终归一化文本按现有 SSE 逻辑分块输出

### 14.3 本轮补齐的测试

核心文件：
- `agents/code-review-agent/tests/test_review_logic.py`
- `agents/code-review-agent/tests/test_contract.py`

本轮补齐并锁定了以下场景：

1. `upstream_openai_compatible` 成功时会真实走上游调用，并把 findings 归一化成当前稳定模板
2. 配置缺失时不会实例化上游 client，直接回退到 `heuristic`
3. 非法 JSON 会严格回退
4. 非法 `severity` / `line_number` 会严格回退
5. 超时异常会严格回退
6. live backend 下 non-stream 接口仍保持当前 HTTP shape
7. live backend 下 `stream=True` 仍保持现有 SSE shape，内容来自最终归一化文本

### 14.4 实际验证结果

已通过：
- `pytest agents/code-review-agent/tests -q`
  - 结果：`46 passed`

### 14.5 当前边界

这轮仍然**没有**进入以下范围：

1. 不做 provider streaming
2. 不改公共 API schema
3. 不暴露上游 provider model 到响应
4. 不透传 provider usage
5. 不接 Gateway
6. 不改 `final-plan`

因此，本轮后的正确口径是：

1. `code-review-agent` 已经从“外部 LLM 预留层”推进到“真实 upstream non-stream 第一段可用”
2. 默认行为仍是本地 heuristics
3. live backend 现在已经具备真实调用与严格回退
4. 下一轮如果继续推进，最自然的是 provider streaming 或更稳的 upstream output governance，而不是跳去 Phase 5

### 14.6 提交

- `26b92e3 feat(code-review-agent): add live upstream fallback`

## 15. Phase 4B `code-review-agent` 已接通真实 provider streaming

在真实 upstream non-stream 调用落地之后，这一轮继续沿着同一条 seam 深化，但仍然没有跳进 Gateway，也没有改公共 API 契约；本轮只把 `upstream_openai_compatible` 在 `stream=True` 下的内部实现，从“先走 non-stream 再本地切块”推进到“真实走 provider stream，再本地归一化输出”。

### 15.1 本轮目标

本轮目标不是改变客户端协议，而是在保持当前 SSE 外壳与稳定文本模板不变的前提下，让 live backend 在流式请求下也真正使用上游 streaming：

1. `POST /v1/chat/completions` 的 request/response shape 不变
2. `stream=True` 继续输出当前 `data: {json}\n\n` SSE 形状
3. `upstream_openai_compatible` 在 `stream=True` 下改为真实调用 provider stream
4. 继续坚持严格回退
5. 成功路径仍然回到当前稳定的 `Summary + Findings + Limitations` 模板

### 15.2 本轮已完成的代码闭环

核心文件：
- `agents/code-review-agent/src/core/upstream_review.py`
- `agents/code-review-agent/src/core/agent.py`
- `agents/code-review-agent/src/endpoints/completions.py`

本轮已完成：

1. 接通真实 provider stream
   - `upstream_review.py` 新增对 `AsyncOpenAI(...).chat.completions.create(..., stream=True)` 的消费路径
   - 通过读取 `choices[].delta.content` 增量收集上游内容
2. 保持稳定模板优先
   - provider stream 会先在服务端被完整消费并校验
   - 只有在 JSON findings 可安全归一化后，才向客户端发出 content chunk
3. SSE 契约保持不变
   - endpoint 仍先发送 role chunk
   - 后续继续输出 content chunk、stop chunk 与 `[DONE]`
4. 严格回退继续成立
   - provider stream 过程中若出现配置缺失、超时、异常、空内容、非法 JSON、非法 findings
   - 都会在首个 content chunk 发出前回退到本地 `heuristic`
5. non-stream 路径不回归
   - `stream=False + upstream_openai_compatible` 继续走上一轮已落地的真实 non-stream 调用

### 15.3 本轮补齐的测试

核心文件：
- `agents/code-review-agent/tests/test_review_logic.py`
- `agents/code-review-agent/tests/test_contract.py`

本轮补齐并锁定了以下场景：

1. `stream=True + upstream_openai_compatible` 会真实以 `stream=True` 调用上游 provider
2. live provider stream 成功时，最终文本与 live non-stream 结果保持一致
3. stream 中途超时异常会在首个 content chunk 前回退到 `heuristic`
4. stream 返回非法 JSON 时会严格回退到 `heuristic`
5. live backend 下 `stream=True` 仍保持当前 SSE shape
6. `heuristic`、`upstream_reserved`、live non-stream 现有行为不回归

### 15.4 实际验证结果

已通过：
- `pytest agents/code-review-agent/tests -q`
  - 结果：`49 passed`

### 15.5 当前边界

这轮仍然**没有**进入以下范围：

1. 不改公共 API schema
2. 不透传 provider usage
3. 不暴露上游 provider model 到响应
4. 不读取本地仓库文件
5. 不做完整 repo-aware 推理
6. 不接 Gateway / `agents.yaml` / `/api/agents/*`

因此，本轮后的正确口径是：

1. `code-review-agent` 已完成真实 upstream non-stream + streaming 两段接通
2. 默认行为仍是本地 heuristics
3. live backend 现在同时具备真实 provider stream 与严格回退
4. 下一轮如果继续推进，更自然的是 repo-aware 深化或上游输出治理，而不是跳去 Phase 5

### 15.6 提交

- `2aec440 feat(code-review-agent): stream live upstream reviews`

## 16. Phase 4B `code-review-agent` 已完成 live upstream findings 治理

在真实 upstream non-stream + streaming 两段都接通之后，这一轮没有扩大到 Gateway，也没有改公共 API；目标只是在保持当前稳定纯文本模板与 SSE 外壳不变的前提下，把 live upstream 返回的 findings 进一步收口到可安全展示的作用域。

### 16.1 本轮目标

1. 继续不改 `POST /v1/chat/completions` 的 request/response shape
2. 不放宽上游 JSON findings schema
3. 把“schema 合法但作用域不合法”的 findings 在本地归一化或丢弃
4. 当 live findings 全部被治理丢弃时，严格回退到 `heuristic`

### 16.2 本轮已完成的代码闭环

核心文件：
- `agents/code-review-agent/src/core/agent.py`
- `agents/code-review-agent/tests/test_review_logic.py`
- `agents/code-review-agent/tests/test_contract.py`

本轮已完成：

1. live upstream findings 作用域治理
   - 丢弃指向 `repo_context` 的 findings
   - single-target review 中，foreign `file_path` 会收口到 `review_target`
   - multi-file diff 中，foreign 文件会直接丢弃
2. 定位治理
   - 越界 `line_number` 会被清空，而不是整包失败
   - 占位路径、无效路径会被归一化处理
3. 去重与回退治理
   - 重复 findings 会去重
   - 若上游原始 findings 非空但治理后全部被丢弃，会严格回退到 `heuristic`
   - 若上游返回 `findings=[]`，则接受 empty findings，不误回退

### 16.3 本轮补齐的测试

本轮补齐并锁定了以下场景：

1. `repo_context` findings 不会直接透出
2. single-target foreign `file_path` 会收口到 `review_target`
3. multi-file diff foreign 文件会被丢弃
4. 越界 `line_number` 会被清空
5. 重复 findings 会被去重
6. stream 场景下治理后全丢弃时，会在首个 content chunk 前回退

### 16.4 实际验证结果

已通过：
- `pytest agents/code-review-agent/tests -q`
  - 结果：`56 passed`

### 16.5 当前边界

这轮仍然**没有**进入以下范围：

1. 不读取本地仓库文件
2. 不做完整跨文件 / 全仓库推理
3. 不透传 provider usage
4. 不暴露 provider model
5. 不接 Gateway / `agents.yaml` / `/api/agents/*`

### 16.6 提交

- `7add3ce feat(code-review-agent): govern live upstream findings`

## 17. Phase 4B `code-review-agent` 已完成 repo context forwarding governance

在 live upstream findings 治理落地之后，这一轮继续沿着同一条 Phase 4B 深化路径推进，但仍然没有跳进 Gateway，也没有改公共协议；目标只是在不读取本地仓库文件的前提下，把调用方显式传入的 `repo_context` 从“原样全量透传”推进到“有选择、有预算、有关系标注的 forwarded context”。

### 17.1 本轮目标

1. 保持 `/v1/chat/completions`、SSE 外壳与 `Summary + Findings + Limitations` 模板不变
2. 不新增任何 `AGENT_*` 配置项
3. 不让 `repo_context` 单独产出 findings
4. 只治理 forwarded context 的输入质量与上游 prompt 形状

### 17.2 本轮已完成的代码闭环

核心文件：
- `agents/code-review-agent/src/core/agent.py`
- `agents/code-review-agent/src/core/upstream_review.py`
- `agents/code-review-agent/tests/test_review_logic.py`
- `agents/code-review-agent/tests/test_contract.py`

本轮已完成：

1. forwarded context 归一化
   - 路径会去空白、去引号、清理 `a/` / `b/` 前缀
   - 与 `review_target` 重复的 context 会被丢弃
   - 同路径 context 只保留第一份非空内容
2. forwarded context 排序
   - 若存在 `review_target_path`，会优先按 `same_dir / same_top_level / same_extension / other` 排序
   - 若不存在 `review_target_path`，则保持原输入顺序
3. forwarded context 预算治理
   - 最多 forward `4` 个 context 文件
   - 单文件最多保留 `80` 行非空内容
   - 全部 context 合计最多保留 `200` 行非空内容
   - 超限时会追加 `... [truncated]`
4. upstream prompt 形状强化
   - prompt 现在会同时声明 `Repo context files supplied` 与 `Repo context files forwarded`
   - 每个 forwarded context 都会显式带上 `relationship` 与 `truncated` 状态

### 17.3 本轮补齐的测试

本轮补齐并锁定了以下场景：

1. 重复 `repo_context path` 时只 forward 第一份内容
2. 与 `review_target` 重复的 context 会被丢弃
3. 在 `4` 文件上限下，更接近 `review_target` 的 context 会优先保留
4. 超长 context 会被裁剪并追加 `... [truncated]`
5. 无 `review_target_path` 时，forwarded context 保持原输入顺序
6. non-stream / stream 下 upstream prompt 都会反映 forwarding governance 结果

### 17.4 实际验证结果

已通过：
- `pytest agents/code-review-agent/tests -q`
  - 结果：`63 passed`

### 17.5 当前边界

这轮仍然**没有**进入以下范围：

1. 不读取本地仓库文件
2. 不做完整跨文件 / 全仓库推理
3. 不改公共 API schema
4. 不透传 provider usage
5. 不暴露 provider model
6. 不接 Gateway / `agents.yaml` / `/api/agents/*`

因此，本轮后的正确口径是：

1. `code-review-agent` 已从 repo-aware 前置输入推进到 repo context forwarding governance
2. `repo_context` 仍不单独产出 findings
3. live upstream 现在不只是“接通并治理输出”，还会对输入上下文做排序、裁剪与显式标记
4. 下一轮如果继续推进，更自然的是 repo-aware 利用层深化或 Gateway 前置设计准备，而不是直接跳去 Phase 5

### 17.6 提交

- `d0085f4 feat(code-review-agent): govern repo context forwarding`

## 18. Phase 4B `code-review-agent` 已完成 repo-aware utilization hints

在 repo context forwarding governance 落地之后，这一轮继续沿着同一条 Phase 4B 深化路径推进，但仍然没有跳进 Gateway，也没有改公共协议；目标是在不读取本地仓库文件的前提下，把 forwarded `repo_context` 从“已被治理并送入上游”再推进到“上游如何使用这些上下文也被显式约束和提示”。

### 18.1 本轮目标

1. 保持 `/v1/chat/completions`、SSE 外壳与 `Summary + Findings + Limitations` 模板不变
2. 不新增任何 `AGENT_*` 配置项
3. 不让 `repo_context` 单独产出 findings
4. 只增强 upstream prompt 的 repo-aware 利用提示，不扩展到本地读仓库或完整跨文件推理

### 18.2 本轮已完成的代码闭环

核心文件：
- `agents/code-review-agent/src/core/upstream_review.py`
- `agents/code-review-agent/tests/test_review_logic.py`
- `agents/code-review-agent/tests/test_contract.py`

本轮已完成：

1. upstream system prompt 利用边界强化
   - 明确 findings 只能针对 `review_target`
   - 明确 `repo_context` 只能作为辅助解释
   - 若风险只存在于 context 且无法回到 `review_target`，必须返回 `{"findings": []}`
2. review scope hint 已落地
   - `unified_diff` 会明确提示“只审查新增行”
   - 非 diff 输入会明确提示“只审查 review target 文本本身”
3. per-context utilization hints 已落地
   - 每个 forwarded context 都会带上 `relationship`
   - 每个 forwarded context 都会带上 `truncated`
   - 每个 forwarded context 现在还会带上 `shared identifiers`
   - 每个 forwarded context 现在还会带上 `usage priority`
4. shared identifiers / usage priority 已有稳定规则
   - 会抽取 `review_target` 与 context 的共享标识符
   - 最多保留 5 个共享标识符
   - 根据 `same_dir / same_top_level / same_extension / other` 与共享标识符数量计算优先级

### 18.3 本轮补齐的测试

本轮补齐并锁定了以下场景：

1. upstream prompt 会带上 `Review scope`
2. upstream prompt 会带上每个 context 的 `shared identifiers`
3. upstream prompt 会带上每个 context 的 `usage priority`
4. `truncated` / forwarded count / supplied count 仍会被正确反映
5. diff 与非 diff 输入会得到不同的 review scope hint
6. shared identifiers 会按稳定顺序裁到最多 5 个

### 18.4 实际验证结果

已通过：
- `pytest agents/code-review-agent/tests -q`
  - 结果：`66 passed`

### 18.5 当前边界

这轮仍然**没有**进入以下范围：

1. 不读取本地仓库文件
2. 不做完整跨文件 / 全仓库推理
3. 不改公共 API schema
4. 不透传 provider usage
5. 不暴露 provider model
6. 不接 Gateway / `agents.yaml` / `/api/agents/*`

因此，本轮后的正确口径是：

1. `code-review-agent` 已从 repo context forwarding governance 继续推进到 repo-aware utilization hints
2. `repo_context` 仍不单独产出 findings
3. live upstream 现在不只会收到治理后的 context，还会收到明确的利用边界与优先级提示
4. 下一轮如果继续推进，更自然的是更进一步的 repo-aware 利用效果评估 / 治理，或 Gateway 前置设计准备，而不是直接跳去 Phase 5

### 18.6 提交

- `b1e4c67 feat(code-review-agent): guide repo context utilization`

## 19. Phase 4B 子后端 readiness 契约缺口已补齐

在 repo-aware utilization hints 落地之后，这一轮没有继续扩 `code-review-agent` 的审查能力面，也没有抢跑 Gateway 实现；目标只是在不碰 `agents.yaml`、`/api/agents/*` 与 repo-aware 规则的前提下，把冻结契约中已经明确存在、但当前样板与首个真实 Agent 还未实现的 readiness 面补齐。

### 19.1 本轮目标

1. 不改 `POST /v1/chat/completions` 协议
2. 不改 repo-aware findings / forwarding / utilization 逻辑
3. 不接 Gateway，不新增 `agents.yaml`
4. 只补 `_template` 与 `code-review-agent` 的 `GET /health/ready`，并让 contract tests 覆盖它

### 19.2 本轮已完成的代码闭环

核心文件：
- `agents/_template/src/endpoints/health.py`
- `agents/_template/src/schemas/response.py`
- `agents/code-review-agent/src/endpoints/health.py`
- `agents/code-review-agent/src/schemas/response.py`

本轮已完成：

1. 冻结契约里的 readiness 端点已补齐
   - `_template` 新增 `GET /health/ready`
   - `code-review-agent` 新增 `GET /health/ready`
2. readiness v1 维持最小静态语义
   - 当前统一返回 `200 {"ready": true}`
   - 不引入动态依赖检查
   - 不读取 provider 状态
   - 不新增配置项
3. 现有 `/health` 保持不变
   - 继续返回 `status / agent / version / uptime_seconds / models`
   - 不把 readiness 混进原 health payload

### 19.3 本轮补齐的测试

核心文件：
- `agents/_template/tests/test_contract.py`
- `agents/code-review-agent/tests/test_contract.py`

本轮补齐并锁定了以下场景：

1. `_template` 的 `/health/ready` 返回 `200`
2. `_template` 的 `/health/ready` body 固定为 `{"ready": true}`
3. `code-review-agent` 的 `/health/ready` 返回 `200`
4. `code-review-agent` 的 `/health/ready` body 固定为 `{"ready": true}`
5. 现有 `/health`、`/v1/models`、non-stream、SSE、auth、live upstream 测试不回归

### 19.4 实际验证结果

已通过：
- `pytest agents/_template/tests -q`
  - 结果：`8 passed`
- `pytest agents/code-review-agent/tests -q`
  - 结果：`67 passed`

### 19.5 当前边界

这轮仍然**没有**进入以下范围：

1. 不接 Gateway / `agents.yaml` / `/api/agents/*`
2. 不引入动态 readiness 判定
3. 不透传 provider usage
4. 不暴露 provider model
5. 不做完整跨文件 / 全仓库推理
6. 不改 repo-aware utilization / findings governance
