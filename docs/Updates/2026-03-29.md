<!-- 编码：UTF-8 -->

# StudySolo 2026-03-29 产品更新履历

## 【面向开发者】

### scripts/ 目录结构整理

对 `scripts/` 文件夹进行了规范化整理，按职责分类归入子目录：

1. **脚本分类归档**：
   - `scripts/startup/` — 启动脚本（`start-studysolo.sh`）
   - `scripts/deploy/` — 部署脚本与生产配置（`deploy-backend.sh`、`deploy-frontend.sh`、`nginx.conf`）
   - `scripts/git/` — Git 同步脚本（保持不变）
   - `start-studysolo.ps1` 因 Windows 常用保留在 `scripts/` 根目录
2. **新增临时目录**：
   - `scripts/logs/` — 运行日志、调试记录存放位置
   - `scripts/temp/` — 临时输出文件存放位置
   - `scripts/sandbox/` — 临时测试脚本存放位置
   - 三个目录均通过 `.gitkeep` + `.gitignore` 保持目录结构但不追踪内容
3. **路径修复**：移动后的 shell 脚本中项目根目录定位已从 `$0/..` 更新为 `$0/../..`
4. **新增 `scripts/README.md`**：完整记录目录结构、脚本说明、端口约定和临时目录使用规范


---

### 项目架构深度审查

结合 `.agent/skills/` 中的 clean-code、architecture、code-review-checklist 规范，对项目进行了全面架构审查。

#### 文件大小违规

前端 `check-max-lines.mjs`（300行限制）检测到 18 个新违规文件，后端有 12 个 Python 文件超过 300 行。

最严重的超标文件：
- `frontend/src/styles/workflow.css` — 1945 行（极严重，需按组件拆分 CSS 模块）
- `frontend/src/features/workflow/components/canvas/WorkflowCanvas.tsx` — 918 行（极严重，需拆分 hooks/handlers）
- `backend/app/engine/executor.py` — 671 行（极严重，需拆分为 topology/node_runner/loop_executor）
- `frontend/src/stores/use-workflow-store.ts` — 453 行（需按职责拆分 slice）
- `backend/app/services/ai_router.py` — 480 行（路由策略和 provider 调用需分离）

#### 架构优点确认

- 前端 feature-based 组织结构合理
- 后端节点插件化设计（按类别分目录）良好
- API 路由聚合清晰，中间件层次分明
- 数据库迁移 baseline + 增量模式规范
- 类型系统注释详尽

#### 待改进项

- `components/layout/sidebar/` 承担过多职责，建议提升为独立 feature
- 后端 `api/` 目录 10 个 admin_* 文件平铺，建议子包化
- 前端 `services/` 13 个文件平铺，缺少按域分组
- 缺少 ADR 记录关键架构决策


---

### 超标文件拆分重构计划

在 `docs/issue/file-size-refactor/` 下创建了完整的逐文件拆分规划：

- `README.md` — 总索引，P0/P1/P2 优先级分级，覆盖前端 18 + 后端 12 个超标文件
- `01-workflow-css.md` — workflow.css 1945 行拆分为 14 个 CSS 模块
- `02-workflow-canvas.md` — WorkflowCanvas.tsx 918 行拆分为 6 个 hooks + 2 个工厂文件 + 1 个子组件
- `03-executor.md` — executor.py 671 行拆分为 topology/node_runner/loop_runner 3 个模块
- `04-ai-router.md` — ai_router.py 480 行拆分为 provider/caller/router 3 层
- `05-workflow-store.md` — workflow store 453 行拆分为 4 个 Zustand slice
- `06-chat-messages.md` — ChatMessages 438 行拆分为 5 个子组件
- `07~18` — 其余 12 个文件的逐一拆分方案
- `ARCHITECTURE-AFTER.md` — 拆分后完整架构 tree + 可复用组件汇总

识别了 8 个跨文件可复用组件（MagicWandLoader、CopyButton、markdownComponents 等）。
总预估工作量约 20 小时。
