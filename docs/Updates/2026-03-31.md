<!-- 编码：UTF-8 -->

# StudySolo 2026-03-31 产品更新履历

## 【面向开发者】

### #01 工作流记忆系统（Workflow Memory）实施完成 + 验证

完成 Workflow Memory 系统的全栈实施，涉及 14 个文件（新建/修改）。代码审查和构建验证通过。

**数据库层**
- 新表 `ss_workflow_run_traces`：节点级执行记录，冗余 `user_id` 实现 O(1) RLS 查询
- 扩展 `ss_workflow_runs`：新增 `is_shared`、`shared_at` 分享控制字段
- RLS 策略：owner 读取、service-role 写入、shared 公开读取

**后端**
- `workflow_execute.py`：执行引擎新增 trace 累积拦截（`_accumulate_trace`），使用 `time.monotonic()` 计时，执行完成后 `_save_traces` 批量写入
- `workflow_runs.py`：5 个 API 端点（按工作流查运行列表、全量列表、详情+traces、公开访问、分享切换）
- 路由注册在 `/api/workflow-runs` 前缀下

**前端服务层**
- `memory.ts` 类型定义 + `memory.service.ts` 客户端 API + `memory.server.service.ts` SSR 双路访问

**前端路由 m/[id]**
- 独立布局复用 `SessionRefresher`，粘性导航栏
- `MemoryView.tsx`：摘要卡片 + 分享开关 + 可展开 TraceCard 列表 + Markdown-lite 渲染
- 空 traces 兼容（记忆系统启用前的历史运行）

**入口集成**
- `RunHistoryPopover.tsx`：画布工具栏运行日志弹出面板（owner only）
- `WorkflowPageShell.tsx` 集成

构建验证：Next.js `✓ Compiled successfully`，`/m/[id]` 路由正常注册。后端模块导入验证通过（5 routes）。


---

### #02 AI 对话模型选择器 IDE 风格改造

- 模型选择器按钮简化为纯文本样式：仅显示模型名称 + 小箭头（`ChevronDown`），移除彩色圆点、边框、背景色
- 下拉菜单改为向上弹出（`bottom-full`），避免在侧边栏底部被裁剪
- 下拉列表精简：移除模型描述行和推荐标签，仅保留模型名称 + PRO 徽章
- 移除底部工具栏的"○ 就绪 / ● 流式"状态指示器，节省空间


---

### #03 修复：执行面板内容溢出无法滚动到底部

根因：`CollapsibleSection` 使用 `max-h-[2000px]` + `overflow-hidden` 做展开动画，当"生成步骤"区域内容超过 2000px 时（多节点展开输出），超出部分被截断且无法滚动。

修复（方案 A+B 组合）：
- `CollapsibleSection.tsx`：`max-h-[2000px]` 替换为 CSS `grid-rows-[0fr]`/`grid-rows-[1fr]` 过渡，内容多高都不会被截断
- `TraceStepOutput.tsx`：输出区域限高 400px + `overflow-hidden`，超出时显示渐变遮罩和"展开全部"按钮；展开后显示"收起"按钮。流式输出模式不限高
