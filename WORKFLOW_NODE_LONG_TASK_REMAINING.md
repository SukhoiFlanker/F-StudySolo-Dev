# 工作流节点系统长任务未完成项与历史遗留汇总

更新时间：2026-03-27

本文档用于汇总这次“工作流节点系统 / SOP 重构 / 简单节点能力补全”长任务中仍未完成的内容，以及在该长任务开始前项目里就已经存在、目前仍未解决的残留任务。本文档仅做事实归档与后续执行参考，不替代正式 SOP、架构文档和阶段计划。

编码要求：
- 本文件必须保持 UTF-8
- 无 BOM
- LF 换行
- 禁止中文转义
- 禁止乱码提交

## 1. 本轮已经完成的主线内容

### 1.1 SOP 与规范层

- 已按真实代码基线修正 `docs/项目规范与框架流程/功能流程/新增AI工具/00-节点与插件分类判断.md`
- 已按真实代码基线修正 `docs/项目规范与框架流程/功能流程/新增AI工具/A型-LLM提示词节点-SOP.md`
- 已按真实代码基线修正 `docs/项目规范与框架流程/功能流程/新增AI工具/B型-外部工具节点-SOP.md`
- 已更新 `docs/项目规范与框架流程/功能流程/新增AI工具/执行面板升级规划.md` 的当前落地状态
- 已新增 `docs/项目规范与框架流程/功能流程/新增AI工具/01-现有节点功能补全总则.md`
- 已把真实 Prompt 装配基线统一到 `backend/app/nodes/_base.py` 三段式拼接方案，不再沿用旧草案中的 `prompt_loader.py`

### 1.2 后端底座

- `/api/nodes/manifest` 已扩展，节点 manifest 已支持：
  - `config_schema`
  - `output_capabilities`
  - `supports_upload`
  - `supports_preview`
  - `deprecated_surface`
- `NodeData` 已新增 `config`
- 执行上下文已补充 `user_id`、`workflow_id`、`workflow_run_id`
- `write_db` 已从占位节点升级为真实写入 `ss_workflow_runs.output.saved_results`
- `trigger_input` 已支持从 `config.input_template` 提供输入模板兜底
- 多个简单节点已补齐 `config_schema` 与基础能力元数据

### 1.3 前端底座

- 已新增节点 manifest 拉取层
- 已新增统一节点配置抽屉
- 已开放节点右键菜单“节点配置”入口
- 已在节点头部增加配置入口按钮
- `AIStepNodeData` 已支持 `config`
- 工作流同步哈希已纳入 `config`
- `write_db` 前端渲染已改为 JSON 展示

### 1.4 知识库节点内化

- 独立知识库页面入口已退役：
  - `frontend/src/app/(dashboard)/knowledge/page.tsx`
  - `frontend/src/features/knowledge/components/KnowledgePageView.tsx`
- 知识库上传、文档列表、处理状态、简单摘要、分块预览已接入节点配置面板
- 上传入口已从独立页面转入节点内

### 1.5 已完成验证

- `frontend` 下 `npx tsc --noEmit` 已通过
- `python -m compileall backend/app` 已通过
- 已跑通一组前端定向 Vitest
- 已跑通一组本次改动文件的定向 ESLint

## 2. 本轮仍未完成的内容

这一部分指的是：本次长任务目标里明确提出过，但目前只完成了部分，或者尚未真正落代码的内容。

### 2.1 简单节点“逐个补全”还没有全部做完

目前完成的是“统一底座 + 配置通路 + 知识库节点内上传 + 一部分节点 schema/manifest 接入”，但还没有把所有简单节点都做到“配置可感知、结果可预览、画布与执行面板一致”。

仍待继续的节点项：

- `flashcard`
  - 已有基础输出能力，但还没有专门做成更完整的节点内学习预览体验
  - 还缺更明确的“首卡摘要、卡片计数、配置驱动展示、执行面板 compact 摘要”的系统验收
- `quiz_gen`
  - 目前主要还是沿用已有作答交互
  - 缺配置项与渲染层的深度联动验证
- `compare`
  - 已补基础 schema
  - 还缺更明确的“对比维度配置驱动预览”和更稳定的摘要区
- `mind_map`
  - 已补基础 schema
  - 还缺更明确的层级展开/失败兜底/节点数摘要统一规范
- `outline_gen`
- `summary`
- `content_extract`
- `merge_polish`
- `chat_response`
  - 上述节点目前主要完成了“纳入统一底座”的工作
  - 还没有逐个完成“配置项是否真正影响输出、compact 结果是否符合执行面板规范、错误信息是否符合双视图要求”的专项补全

### 2.2 控制流节点的体验补全还不完整

- `logic_switch`
  - 已有分支执行引擎
  - 但还没有把“分支配置、默认分支、结果可视化、trace 对齐”做到完整闭环
- `loop_map`
  - 已有列表映射语义
  - 但还没有完整补齐“列表来源摘要、单项数统计、每轮结果更友好的可视化、compact 摘要”
- `loop_group`
  - 当前没有像其他节点那样完整纳入统一 manifest 元数据体系
  - 还缺更系统的容器配置和 trace 可读性补全

### 2.3 B 型工具节点还没有逐个收尾

- `web_search`
  - 还缺“结果数/深度配置真正驱动执行”和更清晰的来源列表预览
- `export_file`
  - 还缺统一的文件名/格式配置体验与更明确的下载结果卡
- `write_db`
  - 第一版已可用
  - 但目前是写入 `ss_workflow_runs.output.saved_results`
  - 还没有更完整的查询展示、键冲突策略验证、管理面板或调试视图

### 2.4 知识库节点还不是完整产品态

- 目前已完成“节点内上传 + 文档处理状态 + 简单摘要/分块预览”
- 但检索仍然主要是按用户维度的已有数据进行工作
- 还没有真正做到：
  - 节点级文档选择过滤
  - 节点级文档绑定
  - 更细的检索参数持久化
  - 更强的知识片段引用可视化
- 本轮明确不扩成独立插件，因此这里只是“最小可用闭环”，不是完整知识管理系统

### 2.5 节点配置框架还只是第一阶段

- 目前配置抽屉已经可用
- 但还没有把所有字段类型和所有节点的配置体验做到完全统一
- 还缺：
  - 更丰富的 schema 控件类型
  - 配置默认值回填规则完善
  - 节点内快捷动作与配置抽屉之间的统一动作层
  - 所有节点都通过 manifest 驱动，而不是部分仍然依赖前端现有元数据

### 2.6 执行面板和节点输出的一致性还没有做最终收口

- 当前已经有 trace drawer 的第一阶段实现
- 但还没有把所有简单节点都逐个对齐：
  - 画布预览
  - 结果 slip
  - 执行面板 compact
  - 错误双视图
- 这部分还需要逐节点验收，而不是只看公共框架是否能工作

## 3. 这次长任务开始前就已经存在、现在仍未解决的遗留任务

这一部分不是本次改动新引入的问题，而是项目原本就存在、此次任务中没有一起清掉的历史残留。

### 3.1 全仓前端 lint 债务仍然存在

当前仍未达到“全仓 `npm run lint` 通过”的状态。已有问题包括但不限于：

- React hooks 相关规则问题
- `set-state-in-effect`
- `refs` 在 render 中被直接读写
- `Date.now()` 的纯度警告
- `static-components` 相关问题
- Next.js 路由中 `<a>` 与 `<Link>` 的规范问题

这些问题分布在多个老文件中，不是本轮单独引入，但仍阻塞“全仓 lint 绿灯”。

### 3.2 前端文件体积与页面体积债务仍然存在

当前仍未达到：

- `npm run lint:lines:strict` 通过
- `npm run lint:pages` 通过

已知问题包括但不限于：

- `frontend/src/styles/workflow.css` 行数过长
- `frontend/src/features/workflow/components/canvas/WorkflowCanvas.tsx` 行数过长
- `frontend/src/stores/use-workflow-store.ts` 行数过长
- 多个 sidebar / page 文件超过项目规定的长度上限

这说明工作流相关模块仍需继续拆分，而不是只补功能。

### 3.3 后端完整测试门禁未跑通

当前未完成：

- `python -m pytest tests`

本次环境里的直接阻塞是：

- 当前 Python 环境缺少 `pytest`

因此现在只能确认：

- 语法级编译通过
- 定向前端测试通过

但不能宣称“后端全量回归通过”。

### 3.4 节点目录与前端元数据仍有“双源”问题

虽然 manifest 已扩展并已被节点配置抽屉使用，但前端节点商店/节点目录/部分视觉元数据仍有相当一部分依赖 `workflow-meta.ts` 或现有前端静态映射。

残留问题：

- manifest 还不是所有节点 UI 的唯一事实源
- 节点商店与运行节点定义仍存在一定程度“双维护”
- 后续如果新增字段，只改后端 manifest 还不能自动覆盖所有前端展示层

### 3.5 旧知识库功能代码还有进一步清理空间

虽然独立页面入口已经删除，但 `frontend/src/features/knowledge` 目录中仍保留了一些为了节点内复用而留下的 hooks、types、utils、列表组件。

这不是错误，但还存在后续工作：

- 继续识别并删除真正只服务旧页面的死代码
- 把“知识库作为独立功能页”的文案与隐式假设清干净

### 3.6 工作流相关大文件的结构治理尚未完成

工作流系统目前仍有若干大文件承担过多职责，尤其是：

- `WorkflowCanvas.tsx`
- `use-workflow-store.ts`
- 若干 sidebar 组件

本轮优先做的是“能力打通”，不是“大规模解耦重构”，所以结构治理还没完成。

## 4. 当前明确存在的环境或流程阻塞

### 4.1 本机缺少 pytest

影响：

- 无法完成后端全量测试
- 无法给出完整回归结论

### 4.2 Git 命令在当前环境不可用

影响：

- 无法直接通过本机命令确认完整 `git status`
- 无法在当前会话里给出严格基于 Git 的改动集合核对

### 4.3 全仓质量门禁仍被历史问题阻塞

影响：

- 即使本轮改动已经局部通过，也不能宣称全仓质量门禁通过
- 后续需要单独安排“历史债务清理批次”

## 5. 建议的后续执行顺序

为了控制风险，后续建议不要再做“大一统大提交”，而是按下面顺序继续：

### 5.1 先做节点能力补全的第二批

优先顺序建议：

1. `flashcard`
2. `quiz_gen`
3. `compare`
4. `mind_map`
5. `web_search`
6. `export_file`
7. `logic_switch`
8. `loop_map`

目标：

- 每个节点都完成
  - 配置可编辑
  - 输出可预览
  - 执行面板 compact 可读
  - 错误信息双视图可读

### 5.2 再做知识库节点的最小增强

建议补齐：

- 节点级文档选择
- 文档绑定配置持久化
- 文档摘要和检索结果之间的可读映射

但仍不要扩成独立插件页。

### 5.3 再做 manifest 单一事实源治理

目标：

- 节点目录
- 节点配置
- 节点视觉元数据
- 节点能力描述

尽量统一从 manifest 或由 manifest 派生，而不是继续前后端双维护。

### 5.4 最后单独开债务治理批次

单独处理：

- 全仓 lint 历史问题
- 行数/页面体积超限问题
- 后端测试环境补齐
- 工作流大文件拆分

## 6. 当前状态结论

这次长任务已经完成了最关键的中段建设：

- SOP 已回到真实架构
- manifest 与节点配置底座已接通
- 知识库上传已回收到节点内
- `write_db` 已有第一版真实能力

但还没有进入“所有简单节点都完全收口”的状态。当前更准确的结论是：

- 统一底座已基本建立
- 单点能力已打通
- 节点逐项补全仍需继续
- 历史质量债务仍然独立存在

后续执行时，必须继续遵守以下约束：

- 保持 UTF-8 编码
- 禁止弄坏中文
- 按更新后的 SOP 分类执行
- 不把简单节点补全再次做成无边界的大重构
